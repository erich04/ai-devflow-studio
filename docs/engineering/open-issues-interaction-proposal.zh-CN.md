# 剩余 Issue 的交互确认稿

以下是待确认方案，目前未修改产品界面。按用户要求，确认后才实施。

## 第一批：恢复可用性

| Issue | 建议行为 |
| --- | --- |
| #60 | 原“同步团队并刷新 snapshot”按钮执行既有同步流程；期间禁用并显示同步中；成功原位显示版本和时间，失败原位显示原因及重试。项目策略跨 Task/Gate 持续展示，普通 Task 的 Gate 评估显示“不适用”。 |
| #65 | 同版本已有成功审查时默认展示结果；次级“重新审查”按钮先确认新记录与费用；失败后的重试保留。并发重复请求由执行层阻止。 |
| #61、#63 | 第一轮保留现有内容顺序，恢复页面/Inspector 的连续纵向滚动，保证底部内容、标签和按钮可访问。 |

## 第二批：简单的 Web 调整

| Issue | 建议行为 |
| --- | --- |
| #51 | 顶部提供“跟随系统 / 浅色 / 深色”选择；默认跟随系统，保存到当前浏览器。刷新后保留，首次绘制使用已保存主题。 |
| #53 | Work Request 与相邻卡片对齐，最大宽度一致。空状态放在表单下方，不留独立空白列表列；有请求时显示列表，窄屏按阅读顺序堆叠。 |
| #54 | 配对码独占完整宽度，下方单独一行放“复制”与“撤销”；完整值仍可复制，不新增强制隐藏/显示步骤。 |
| #58 | 应用推荐策略期间显示处理中并防重复；成功重新读取服务端策略，展示名称、版本和时间；已应用时显示“已应用”；失败原位说明。 |
| #59 | 删除标题栏指向自身的伪操作链接，保留真正的策略提交按钮。 |

## 第三批：需要决定入口与信息组织

- #52：项目选择区的“创建项目”在当前 Studio 内打开表单，创建成功自动选择项目。把预算配置/审批迁入 Studio 的设置入口；团队成员、成本、最近 Run 放入团队总览。正常导航不再跳到旧壳，旧壳只保留明确标记的诊断用途，完成迁移清单验收后退役。
- #57：在 Studio 设置内提供 Policy 页面，先展示当前来源和全部规则；Owner 编辑，Lead/Member 只读；保存前预览规则变化，保存后显示服务端结果和 Desktop 同步提示。项目覆盖策略不伪装成已支持。
- #55：已保存 Provider 旁增加次级管理入口。删除先展示 Provider 名称、模型、引用及影响；有执行/项目配置引用时阻止并提示解除方法；可删除时二次确认。删除当前项后保持未选择状态，不自动切换 Provider。
- #64：需在两种方案中选定：A. 所有节点统一“产物 / 测试证据 / 轨迹”，Gate 补充产物入口，各计数点击后进入对应标签；B. Gate 卡片直接区分“审查 / 测试 / 轨迹”，审查计数进入审查证据。两者都保证计数范围与目标内容一致。

## 验收方式

每个已确认方案先补复现或回归检查，再改产品。验证常用视口、窄屏、长内容、键盘访问、成功与失败反馈；真实 Provider 验收与受控自动化分别记录。未确认的交互不实施，未验证的 Issue 不关闭。
