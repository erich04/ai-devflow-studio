# DevFlow Studio V1.4 Pilot Trust Boundary — Execution Plan

Lifecycle: Completed and released as `v1.4.0`; current status lives in `docs/roadmap.md`

Historical execution snapshot: Implementation complete; V1.4 candidate preparation in progress and formal signoff pending

Baseline: `v1.3.0` / `06f3cc321300e3751aaa41c67f66d70cfaf6ebe4`

Development base: `b7b879c20f802dc8e4f2e27eb5413263b9998e37`

## Objective

Deliver the V1.4 scoped PRD through small, independently verifiable vertical slices while
preserving V1.3's canonical local state, scope immutability, redaction, evidence ordering, and human
Gate authority.

## Fixed Decisions

- Durable remote-sync outbox is a V1.4 release Must.
- An absent budget policy is not the same as an authenticated lead explicitly saving
  `enabled: false`.
- A real paid application runtime without a valid authoritative decision is blocked. The
  release-only provider smoke remains a separate explicit operation.
- Existing unsigned-header protections are tested and packaged safely; they are not rewritten
  without a demonstrated bypass.
- GitHub delivery stays in V1.5.
- Pilot packaging is unsigned and excludes installers, signing, notarization, and auto-update.
- API knowledge sourcing and Web/Desktop write authority require ADRs before their write paths land.

## Working Discipline

- Develop in `codex/v1.4-pilot-trust-boundary` from the clean post-V1.3 baseline.
- Do not stage or edit the user's modified guides, research note, `.zcode/`, `module.yaml`, or
  existing `work/` content in the main worktree.
- Use one RED → GREEN behavior at a time. Refactor only while green.
- Default tests must not call a paid provider.
- Do not start Docker until a Postgres, outbox, Web/API integration, or lifecycle slice needs it.

## Phase 0 — Executable Contract

- [x] Reconcile the roadmap with the completed V1.3 release.
- [x] Create the V1.4 scoped PRD.
- [x] Decide that durable outbox is a release Must.
- [x] Record the paid-runtime decision matrix and hard stops.
- [x] Add contract tests that keep the roadmap/PRD/plan aligned.

Stop if a release Must, authority boundary, or failure semantic is still ambiguous.

## Slice A1 — Coding Runtime Paid Budget Fail-Closed

### Tracer 1: guard dependency missing ✅

RED:

- A real paid Coding Engine without a budget guard returns a failed Coding Run before provider
  initialization/invocation.
- The failure is persisted as a redacted Run/Event and leaves no managed worktree.
- Fake/no-cost Coding remains usable without a guard.

GREEN:

- Replace the generic missing-guard allow fallback with a cost-aware unavailable decision.
- Use a precise unavailable summary rather than claiming every failure needs lead approval.

### Tracer 2: Team decision unavailable or invalid ✅

RED:

- Unpaired project, timeout/network error, 401/403, 5xx, and malformed 2xx budget response all block
  before provider initialization/invocation.

GREEN:

- Extract the Electron budget guard from `main.ts` behind a small testable interface.
- Add `unavailable` to the shared decision vocabulary.
- Runtime-validate the remote budget response before returning it to the runtime.

### Tracer 3: policy intent ✅

RED:

- Missing policy blocks as unavailable.
- An explicitly saved disabled policy remains an authenticated allow decision.
- Enabled policy allowed/warning/over-budget behavior remains unchanged.

GREEN:

- Separate missing from explicitly disabled policy in the shared evaluator.

### Tracer 4: approval scope ✅

RED:

- Wrong project, provider, requester, role, status, cost ceiling, or expiry cannot authorize a paid
  call.

GREEN:

- Bind approval validation to project and provider in addition to the existing fields.

### A1 hard stops

- No real paid Coding path may turn missing guard, pairing, auth, network, server, policy, or response
  validation failure into `blocksRun: false`.
- `engine.ensure` and `engine.start` are not called after a rejecting decision.
- A rejected attempt leaves no managed worktree.
- Fake/no-cost behavior remains explicit and green.
- No real provider is called by tests.

## Slice A2 — Knowledge Review Paid Preflight And Audit ✅

- Add the same paid decision contract before Electron and API `reviewKnowledge` invocation.
- Persist or server-audit rejected decisions without creating a fake successful Agent Review.
- Bind approvals to the exact project/provider/requester/cost/expiry scope.
- Cover fake/no-cost, missing policy, unavailable, malformed, over-budget, and valid approval paths.

The paid-runtime trust boundary is complete for the V1.4 Coding and Knowledge Review paths.

## Slice A3 — Authentication And Audit Closure ✅

- Prove unsigned headers are unavailable by default and for browser origins.
- Make pilot configuration reject `DEV_AUTH_ENABLED=true`.
- Audit remaining 401/403, membership, role, pairing, budget, and approval failures with redacted
  reasons.
- Fix code only where a failing negative test demonstrates a bypass.

## Slice B — Durable Remote-Sync Outbox ✅

- [x] Introduce versioned SQLite migration support before adding the outbox table.
- [x] Persist operation/entity IDs, stable idempotency key, status, attempt count, next attempt, and
  redacted error; never persist raw upload payloads.
- [x] Rebuild summaries from canonical LocalStore state at send time.
- [x] Add capped exponential backoff, restart recovery, terminal-error classification, manual retry,
  and operator-visible state.
- [x] Keep project binding immutable and preserve the exact V1.3 child-first/canonical-missing
  recovery.

## Slice C — Repository Knowledge Integration ✅

- [x] Build a bounded Electron main-process Markdown index service with path, symlink, size, and count
  controls.
- [x] Feed one consistent snapshot into Gate, Knowledge Review, Coding Brief, and the Knowledge UI.
- [x] Preserve the ADR rule that retrieval references are not Governance evidence.
- [x] Decide API knowledge provenance in an ADR before connecting API Review.

## Slice D — Web Management Closure ✅

- [x] Land explicit project-scoped Run selection and the existing pairing panel first.
- [x] Record Work Request and Gate command authority in ADR 0012 before adding write paths.
- [x] Add the versioned Work Request create, claim, recovery, local materialization, and Inbox flow.
- [x] Add the versioned Gate Command preflight, delivery, local apply, acknowledgement, and Web flow.
- [x] Require signed session, membership, role, idempotency, expected versions, and server-side
  evaluation; do not duplicate the Electron workflow state machine.

## Slice E — Reproducible Pilot Lifecycle ✅

- [x] Define production API/Web images or bundles, environment allowlist, health contract, and
  migration entrypoint.
- [x] Build and smoke the reproducible unsigned Desktop pilot artifact without expanding scope into
  signing, notarization, installers, or auto-update.
- [x] Prove fresh deploy, retained-data upgrade, failed-upgrade recovery, and documented rollback
  compatibility.
- [x] Keep lifecycle smoke separate from default `verify` where it requires Docker or mutable
  services.

## Verification Ladder

For each tracer:

1. Run the one new targeted RED test and record the expected failure.
2. Make the smallest GREEN implementation.
3. Re-run the targeted file.
4. Refactor only while green.

For a completed slice, run the relevant superset:

- `corepack pnpm typecheck`
- `corepack pnpm test`
- `corepack pnpm test:cross-platform`
- `corepack pnpm build` when package output changes
- `corepack pnpm test:electron-smoke` for Desktop end-to-end behavior
- `corepack pnpm test:postgres-smoke` for API/Postgres persistence or auth changes
- `corepack pnpm test:docker-smoke` for pilot stack/lifecycle changes
- `corepack pnpm test:docker-lifecycle-smoke` for fresh, retained-upgrade, failed-upgrade recovery,
  and bounded application rollback behavior
- `corepack pnpm build:desktop-pilot` followed by `corepack pnpm test:desktop-pilot-smoke` for the
  unsigned Desktop pilot artifact

Any real paid smoke remains release-only and requires separate explicit cost authorization.

## Progress Log

- 2026-07-31: V1.3 signed and released; post-release annotated-tag checkout fix reached `main`.
- 2026-07-31: V1.4 scope audited against roadmap, baseline PRD, implementation, and V1.3 deferrals.
- 2026-07-31: Cursor Grok 4.5 reviewed the initial plan in read-only Ask mode. Codex accepted the
  missing decision matrix/outbox/HARD STOP findings, rejected the unsupported blanket treatment of
  an explicitly disabled policy, and independently added malformed-response and approval-scope
  requirements.
- 2026-07-31: Phase 0 PRD and execution plan created; A1 TDD is next.
- 2026-07-31: V1.4 documentation contract test passed (3/3).
- 2026-07-31: Slice A1 completed. Paid Coding Runtime now makes an authenticated, runtime-validated
  project/provider-scoped budget decision before engine initialization or worktree creation;
  unavailable decisions persist redacted local evidence and surface recovery guidance without a
  false approval action. Fake/no-cost runtime remains explicit. Slice A2 is next.
- 2026-07-31: Slice A2 completed. Desktop and API Knowledge Review now use a bounded 1,024-token
  output reserve and the same project/provider/requester/cost/expiry budget contract before any
  credential decryption or provider call. Blocked Desktop attempts persist one redacted local error;
  blocked API attempts persist a redacted standalone Agent Event and never fabricate a successful
  Review. Exact built-in fake review remains the only no-cost bypass. Full unit regression passed
  705/705, workspace typecheck/build passed, and cross-platform checks passed. Slice A3 is next.
- 2026-07-31: Slice A3 completed. Unsigned identity headers remain disabled by default and are now
  rejected case-insensitively for every browser Origin. Invalid credentials return a redacted 401,
  repository-backed authentication outages return a distinct redacted 503, and membership/role
  failures remain distinct 403 responses. The API rejects unsigned development auth for pilot or
  non-loopback deployments before repository initialization. Docker pilot smoke now uses a signed
  Session Cookie to create a pairing code and a scoped Bearer Token for sync; it no longer enables
  demo headers. Full unit regression passed 716/716, workspace typecheck/build passed, and
  cross-platform checks passed. Slice B is next.
- 2026-08-01: Slice B completed. SQLite schema v9 now persists metadata-only Run, Test, Review, and
  Coding outbox operations atomically with canonical local mutations. A single-instance Electron
  main process reconstructs summaries by ID, freezes pairing credentials and scope per attempt,
  enforces a bounded request timeout, retries transient failures with capped backoff, recovers
  expired leases after restart, and preserves the exact child-first canonical-Run recovery. The
  renderer receives only safe operation metadata and can retry a terminal operation by ID; legacy
  direct summary upload surfaces were removed. Full unit regression passed 870/870, workspace
  typecheck/build, cross-platform checks, and the real Electron smoke passed without Docker or a
  paid provider. Slice C integration is next.
- 2026-08-01: Slice C foundation landed. The Electron main-process repository index accepts only a
  trusted Local Project, reads Git-managed Markdown with path/symlink/file/count/byte/character/chunk
  limits, emits stable SHA-256 snapshots and fixed warning codes, and has 13 focused tests. ADR 0011
  fixes V1.4 API knowledge provenance at `none` and prohibits implicit Desktop Markdown upload.
- 2026-08-01: Slice C completed. A per-project single-flight cache supplies one stable repository
  snapshot to Gate, Knowledge Review, Coding Brief, retry, and renderer flows. Review and Coding
  prompts use bounded, redacted chunk text with source path, section, retrieval strategy, score, and
  content hash; the renderer preserves last-good state, isolates project switches, and bounds graph
  rendering. Metadata amplification, file-growth reads, storage-error disclosure, and raw chunk
  adapter exposure have focused negative coverage.
- 2026-08-01: Web D1 completed. The Team shell now requires stable URL-selected project and Run scope,
  never falls back to a global Run or Review, and creates copy-once Desktop pairing codes only for
  the selected project. API overview and Review reads are constrained by organization, membership,
  project, and visible Run, including real Postgres joins. ADR 0012 fixes Team Work Request/Gate
  Command ownership and Desktop canonical Run authority. Full unit regression passed 944/944.
  Work Request and Gate Command implementation is next.
- 2026-08-01: The Work Request vertical slice completed. Signed-session Web creation and scoped
  Desktop Bearer list/claim/materialize routes now hand off to a narrow Electron IPC surface. SQLite
  schema v10 atomically persists the canonical Run, initial evidence, run-summary outbox, and an
  immutable Work Request binding. Deterministic Run identity plus `claim_pending` and lost-response
  recovery prevent duplicate or ghost Runs across crashes; an acknowledged request reopens the
  current advanced Run rather than its creation snapshot. Renderer and main process calls are
  single-flight, remote requests have a bounded abort deadline, and no token or raw remote payload is
  stored in the binding. Full unit regression passed 1162/1162, workspace typecheck and the Desktop
  production build passed without Docker or a paid provider.
- 2026-08-01: Slice D completed. Signed-session Web commands now pass project-scoped membership,
  role, idempotency, expected Run/policy version, and server preflight checks before a paired Desktop
  can lease them. Desktop applies the command only through the canonical local Gate authority,
  persists a receipt, and returns a bounded acknowledgement without mutating the Team Run projection.
  The real Postgres and Docker vertical smokes completed the command lifecycle with
  `human_rejected` plus acknowledgement.
- 2026-08-01: Slice E completed. The production Postgres migration bundle now reaches schema v10,
  while Desktop SQLite reaches schema v12. Real Postgres and Compose smokes passed, as did fresh
  schema-v10 deployment, retained V1.3-to-V1.4 data upgrade, transactional failed-upgrade recovery,
  and a bounded V1.3 API read rollback against the upgraded database. The reproducible unsigned
  Desktop pilot artifact also built and passed its packaged smoke.
- 2026-08-01: A Computer Use walkthrough exercised Web Work Request creation, Desktop claim and
  materialization, local workflow advancement, Web Gate rejection, Desktop receipt, local apply, and
  Team acknowledgement. The first pass correctly failed closed because projectized effective policy
  projections omitted `projectId`; focused seed, Postgres, and route tests reproduced the issue, all
  project projections were corrected, and the repeated walkthrough reached `human_rejected` with an
  acknowledged receipt.
- 2026-08-01: V1.4 scoped implementation is complete. Package metadata intentionally remains
  `1.3.0`; candidate SHA formation, version alignment, candidate-bound evidence, and formal V1.4
  signoff remain separate pending work. This plan does not claim that V1.4 is released or signed.
- 2026-08-06: Candidate preparation started under `docs/plans/v1.4-release-signoff.md`. Version
  alignment occurs during candidate formation after the release tooling and current documentation
  are reconciled. Candidate-bound validation, signoff, the tag, and the Release are not yet claimed.
