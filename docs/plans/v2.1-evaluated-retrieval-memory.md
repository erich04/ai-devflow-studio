# V2.1 Evaluated Retrieval And Memory Implementation Plan

Status: Complete — immutable candidate-bound evidence recorded

Date: 2026-08-13

Roadmap position: Completed second 2.x milestone after V2.0

Contract: `docs/product/prd/v2.1-evaluated-retrieval-memory-prd.md`

Architecture: ADR 0007, ADR 0017, and ADR 0018

## Delivery Progress

| Slice | Status | Current evidence |
| --- | --- | --- |
| Slice 0 | Complete | Accepted ADRs, PRD, stable vocabulary target, versioned corpus, executable plan, Roadmap promotion, and RED→GREEN document contract. |
| Slice 1 | Complete | Versioned scoped requests, finite lexical/vector candidates, exact Citations, strict no-cost corpus parsing, deterministic lexical metrics, 37 focused tests, full typecheck/unit, and cross-platform serialization passed. |
| Slice 2 | Complete | Deterministic fixture vectors, RRF merge, admitted-set reranking, exact current Citation validation, and frozen quality comparison pass with zero paid-provider or isolation violations. |
| Slice 3 | Complete | Desktop schema 22 retained migration, atomic current-snapshot activation, source identity invalidation, strict bounds, and explicit local-only rebuild. |
| Slice 4 | Complete | Desktop schema 25, inert candidates, exact promotion/revision/deletion capabilities, immutable revision history, scoped retrieval, conflict/expiry filtering, tombstone-first deletion, restart-safe purge, and old-replay fencing. |
| Slice 5 | Complete | Desktop schema 26 atomically persists bounded main-owned Context, binds Runtime contextDigest, fences stale Citation/Memory again at durable capability grant reservation, exposes renderer projection v2 metadata-only provenance, completes exact human Memory actions, and proves packaged cold restart with zero repeated retrieval/provider/Tool/Memory effects. |
| Slice 6 | Complete | Team schema 18 stores one strict monotonic metadata-only projection with independent lifecycle and quality versions; Desktop schema 27 delivers it through an ID-only durable outbox; Web renders only redacted lifecycle and quality metadata. |
| Slice 7 | Complete | Candidate `2c11faa09f35c4930b3c4cf489469b696ccd84ea` passed the frozen evaluator, full local/cross-platform/packaged matrix, first-attempt five-job Verify, and exact direct-child completion evidence. |

## Delivery Rules

- Use one observable RED → GREEN tracer at a time; refactor only when green.
- Keep each commit independently reversible and the branch green before the next slice.
- Preserve ADR 0007, deterministic Workflow/Gate authority, V2.0 Runtime bounds, and Desktop/Team
  placement.
- Scope and deletion filter before embedding, ranking, reranking, Memory attachment, or provider use.
- Default CI is deterministic/no-cost; real providers are opt-in, bounded, and separately evidenced.
- Do not widen V2.1 into V2.2 Multi-Agent coordination.

## Protocol Defaults And Hard Maxima

| Bound | Default | Hard maximum |
| --- | ---: | ---: |
| Query text | 2 KiB | 8 KiB |
| Candidate chunks before rerank | 32 | 128 |
| Returned citations | 5 | 20 |
| One local chunk | 16 KiB | 64 KiB |
| Embedding dimensions | fixture-defined | 4096 |
| Memory candidate text | 2 KiB | 8 KiB |
| Active Memory revisions per scope | 256 | 2048 |
| Evaluation cases | corpus-defined | 512 |

Renderer/model input may lower a request limit but cannot raise these maxima or change scope.

## Slice 0 — Contract Freeze

Outcome: the accepted architecture, product scope, vocabulary target, versioned evaluation corpus,
and executable sequence exist before V2.1 product code.

RED:

- Add `scripts/v21-plan-doc.test.ts`; observe the Roadmap cannot resolve the exact V2.1 contract set.

GREEN:

- Add ADR 0017, ADR 0018, the V2.1 PRD, this plan, the versioned corpus, and exact Roadmap links.
- Record that V2.1 product code begins with Slice 1; no vector database or provider is selected.

Verification: focused document/corpus tests, full typecheck, and `git diff --check`.

Commit boundary: contracts and fixture corpus only.

## Slice 1 — Shared Retrieval, Citation, And Evaluation Contracts

Outcome: pure versioned contracts reject malformed or out-of-scope data without I/O.

RED → GREEN tracers:

1. Parse one exact scoped retrieval request; reject unknown fields, bad bounds, and missing scope.
2. Accept one finite lexical/vector candidate set; reject duplicate/wrong-scope/dimension/stale hash.
3. Produce one exact Knowledge Citation; reject fabricated or stale snapshot/chunk identity.
4. Parse the frozen corpus and compute lexical baseline metrics deterministically.

Verification: shared focused tests, corpus parser tests, property/boundary tests, typecheck, full unit,
and cross-platform serialization.

Commit boundary: shared pure contracts/evaluator only; no persistence/provider.

## Slice 2 — Deterministic Hybrid Retrieval

Outcome: one local no-cost hybrid retriever improves the frozen semantic cases and preserves exact
scope/citation behavior.

RED → GREEN tracers:

1. Merge lexical and fixture-vector candidates with deterministic tie breaks.
2. Rerank only the admitted scope; a foreign candidate is rejected before scoring.
3. Refresh changed content hashes and reject stale citations.
4. Compare lexical and hybrid metrics on the frozen corpus with zero isolation violations.

Use deterministic fixture embeddings first. A provider adapter may follow only behind the existing
credential, budget, redaction, deadline, and explicit opt-in boundary.

Completion evidence: 43 focused shared tests and shared typecheck pass. On the frozen corpus,
Recall@K, nDCG@K, and mean reciprocal rank all reached `1.0`; aggregate improvement over lexical was
`0.5`; citation precision and faithfulness were `1.0`; isolation violations and paid-provider calls
were zero.

Commit boundary: in-memory hybrid retrieval and evaluator behavior only.

## Slice 3 — Durable Local Retrieval Index

Outcome: Electron main owns crash-safe, project-scoped vector/index state.

RED → GREEN tracers:

1. Migrate retained Desktop schema 21 to schema 22 with zero fabricated rows.
2. Build/activate a new exact snapshot atomically; crash leaves the previous snapshot current.
3. Source update/delete invalidates chunk/vector/citation identity before later retrieval.
4. Corrupt/dimension/model/scope mismatch fails closed and rebuild remains bounded.

No Team schema change occurs in this slice.

Completion evidence: retained Desktop schema 21 → 22 migration with zero fabricated index rows;
atomic replacement leaves the old snapshot current when durable persistence fails; source update and
delete remove stale current identities; wrong-model, wrong-dimension, non-finite, over-1024-chunk,
and cross-scope state fail closed; and explicit bounded rebuild restores only derived index state
while preserving the Local Project and Run. The final focused matrix passed 143 focused local-store
tests and 43 focused shared retrieval tests. Repository verification passed 192 test files and 2729
tests, all workspace type checks, the Next production build, OpenCode smoke, and cross-platform
Desktop checks.

Commit boundary: Desktop schema 22 and Electron-main-owned retrieval index persistence only; no Team
schema or provider change.

## Slice 4 — Scoped Agent Memory Lifecycle

Outcome: main-owned Memory is curated, versioned, isolated, expiring, and deletion-safe.

RED → GREEN tracers:

1. Create an inert Memory Candidate from one accepted observable result.
2. Promote through exact authority; reject model/renderer/self-promotion and forbidden content.
3. Revise with optimistic concurrency; preserve explicit conflicts and immutable history.
4. Exclude cross-scope/expired/tombstoned/purge-pending rows before ranking.
5. Delete, restart, purge derived index state, and prove old replay cannot resurrect Memory.

Completion evidence: Desktop schema 22 → 23 added empty inert candidate storage, schema 23 → 24
added revision/head/tombstone/derived-index/audit storage without fabricated lifecycle rows, and
schema 24 → 25 retained promotion history while removing the invalid one-revision-per-candidate
constraint. Main-owned opaque capabilities now gate promotion, revision, and deletion; exact head
CAS preserves immutable history and explicit conflicts; retrieval filters scope, visibility, expiry,
tombstones, and purge state before returning content; and deletion survives restart before derived
index purge while old authorized revisions cannot resurrect it. The final focused matrix passed 152
focused local-store tests and 48 focused shared retrieval/Memory tests. Repository unit verification
passed 192 test files and 2743 tests after the active-Slice document contracts advanced to Slice 5.

Commit boundary: local Memory kernel/store/IPC only; no Team projection.

## Slice 5 — Agent Runtime And Desktop UX

Outcome: Knowledge Citations and Memory revisions enter bounded Context with visible provenance.

RED → GREEN tracers:

1. Attach exact current citations/revisions and record only allowlisted trajectory metadata.
2. Stale/delete/revoke after checkpoint fences continuation before another external action.
3. UI distinguishes Knowledge, Working Memory, Candidate, Durable Memory, conflict, expiry, and
   deletion; human actions use exact versions.
4. Packaged restart preserves accepted Context and repeats zero retrieval/provider/Tool effects.

Current implementation evidence: Desktop schema 25 → 26 adds an empty retained migration and a
bounded full Context attachment owned only by Electron main. Runtime creation and attachment commit
atomically, cold restart reopens the same attachment, Runtime `contextDigest` equals the attachment
digest, and every external Tool action rechecks current snapshot/chunk identity, Memory head/revision,
tombstone, expiry, and Team pairing before invocation and again at durable capability grant
reservation. Empty retrieval results remain valid bounded Context. Renderer projection v2 exposes
only the attachment ID, Context/identity digests, and Knowledge Citation and Durable Memory counts;
source paths, bodies, scope sessions, and the full attachment remain in Electron main. The focused
Context matrix passes 155 local-store tests, 18 Desktop Runtime tests, 8 shared Context contract
tests, 3 renderer-projection tests, 1 renderer-access test, 1 console-state test, and 3 Runtime-panel
tests. The Desktop now also binds one selected Run and persisted Runtime to a complete main-derived
user/session/Local Project scope, rechecks the canonical Run and exact current pairing before and
after row access, and exposes one main-owned Agent Memory lifecycle access. Its bounded strict renderer
contract distinguishes Working Memory, Candidate pending/promoted, Durable
active/conflict/expired/purge/deleted, and exact
revision/head versions; deleted statements, scope sessions, opaque capabilities, authority digests,
raw output, and local paths never enter the renderer. One human action now promotes only a pending
Candidate from the exact selected Runtime using the renderer-observed Candidate ID plus content and
provenance digests. Electron main derives the fixed private user-project authority, consumes the
opaque capability, and returns only the refreshed strict projection. A second human action revises
only the exact active Memory from the selected Runtime by binding its current revision/head versions,
content/provenance digests, and bounded replacement statement while Electron main preserves its
visibility, sensitivity, retention, expiry, and complete scope authority. Exact-version
deletion now requires explicit confirmation and the same exact version/digest boundary; Electron
main builds the human authority, persists the tombstone before purge, and can resume an
exact pending purge after restart without creating deletion authority again. A successful accepted
Runtime observation and its inert Candidate now commit atomically. The packaged Desktop then uses
only strict IPC to promote revision 1, revise to revision 2, tombstone and purge it, and cold-reopen
the exact deleted lifecycle. Local MCP and native Coding Tool counts remain exact, the Memory audit
has one row for each lifecycle transition, the derived index is empty, and
`memoryRestartDuplicateEffects` is zero. The lifecycle matrix passes 4 shared projection tests, 6
main-owned access tests, 6 main-owned human-action tests, and 7 panel tests. Repository unit
verification now passes 197 test files and 2790 tests.

## Slice 6 — Redacted Team Projection

Outcome: Team can inspect quality and lifecycle metadata but cannot read or control local content.

Team schema 17 adds empty `agent_memory_summaries` and `agent_memory_projection_audits` tables with
bounded citation IDs, lifecycle/version/count metadata, redacted-only rows, and no local statement,
content digest, prompt, reasoning, credential, path, raw output, Local Project, or session field.
Team schema 18 adds `qualityVersion` without changing that content boundary: lifecycle
`headVersion` and accepted-Context quality advance independently, and audit identity is the exact
`(memoryId, headVersion, qualityVersion)` tuple. Fresh and retained migration contracts fabricate zero
Memory projection rows. The strict shared
contract binds one Team Project, canonical Run/Node, source Runtime, owner/candidate identity,
revision/head/lifecycle, provenance digest, at most 64 canonical citation request IDs, and bounded
quality counts. `acceptedContextCount` cannot exceed `retrievalCount`; because rejected retrievals
have no durable record, the current Desktop source counts only retrievals durably accepted into a
Runtime Context and reports the two counts equally rather than fabricating attempts. New projections
start `qualityVersion` at 1 and advance it to `acceptedContextCount + 1`; reserved version 0 marks
only retained schema-17 rows so their old digest can converge on the first post-upgrade sync.

Desktop schema 27 retains every schema 26 outbox row while adding `agent-memory-summary`. Each local
lifecycle mutation and accepted Context attachment atomically coalesces one metadata-only ID outbox
operation; the sender re-derives the current projection from Electron-main-owned state, rechecks the
canonical Run/Node and current Team pairing, and uploads through the exact summary endpoint. Seed and
Postgres enforce fixed scope, independently monotonic head/quality state, idempotent composite audit identity, and no
cross-project write. Web strictly parses the same allowlist and shows citation count, accepted
Context count, revision, lifecycle, visibility, sensitivity, and retention. Web remains read-only:
it cannot promote/delete local Memory, rebuild an index, or resume Runtime.

RED → GREEN tracers:

1. Team schema 18 retained migration stores no local content, preserves the empty v17 projection,
   and installs independent quality audit versioning without fabricating a projection.
2. Sync exact monotonic allowlisted metrics/citation IDs/Memory lifecycle summaries idempotently.
3. Reject source/chunk text, prompt, reasoning, credentials, paths, raw output, and cross-project data.
4. Web is read-only; Team cannot promote/delete local Memory, rebuild an index, or resume Runtime.

All four tracers and the Slice 7 candidate gate are complete. The immutable evidence binds the final
full-repository, real-Postgres, Docker, cross-platform, packaged, redaction, and evaluation results.

## Slice 7 — Evaluation And Completion Gate

Outcome: immutable candidate-bound evidence proves improvement and safety.

- Freeze exact corpus/contract digests and execute every named deterministic case.
- Require retrieval improvement, citation floors, Memory task improvement, zero paid calls, zero
  isolation/redaction/deletion violations, and exact candidate binding.
- Run full verify/build, real Postgres, Docker lifecycle, Windows, macOS, Electron, packaged restart,
  retained migration, and artifact integrity gates.
- Commit only the exact completion records and truth-document status changes as the clean direct child
  of the passing candidate.

The candidate-bound command is `corepack pnpm test:v21-retrieval-memory-evaluator`. It runs with
provider and credential authority removed, derives the exact corpus and combined contract digests,
compares lexical with hybrid retrieval, and compares no-Memory with scoped active Memory. It fails
closed on any metric tamper, paid-provider call, lifecycle leak, tenant leak, deletion resurrection,
source content, raw output, absolute path, or secret evidence.

After the exact candidate passes the local and first-attempt Verify matrices, its immutable direct
child adds only:

- `docs/releases/v2.1.0/retrieval-memory-evaluation.json`
- `docs/releases/v2.1.0/required-gates.json`
- the V2.1 plan, PRD, Roadmap, and README truth-status changes.

`corepack pnpm v21:completion-status` independently verifies that direct-child file set, exact
candidate identity, corpus/contract digests, evaluator summary, first-attempt five-job Verify run,
Desktop artifact, recording order, evidence immutability, and clean worktree.

## Completion Evidence

Candidate `2c11faa09f35c4930b3c4cf489469b696ccd84ea` passed the no-cost evaluator, full local
matrix, exact-SHA Verify attempt 1, and exclusive Desktop artifact verification. The immutable
records are `docs/releases/v2.1.0/retrieval-memory-evaluation.json` and
`docs/releases/v2.1.0/required-gates.json`. V2.2 may now begin without widening the completed V2.1
retrieval, Citation, Memory, or authority contracts.
The historical implementation markers `Status: Active — Slice 7 in progress` and
`| Slice 7 | In progress |` are preserved only as audit vocabulary for the pre-signoff phase; the
authoritative status and slice table above supersede them.
