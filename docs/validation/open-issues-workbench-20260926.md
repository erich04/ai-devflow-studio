# Open Issue 分批修复验证记录（2026-09-26）

## 范围与当前状态

基线为 `658c511658daa21cc716f10b5a3f0470adecd0b5`。用户要求实际解决 #135、#171、#172、#173、#174、#175、#176、#177，并用真实小需求跑通全流程后再决定关闭。所有 Issue 仍保持 open；本记录不把测试副本、受控模型或仅有产物当作完整验收。

候选实现为 `075e711`、`a876dd8`、`35f9981`、`6884d09`。最新代码已通过 [CI 36248411908](https://github.com/erich04/ai-devflow-studio/actions/runs/36248411908) 的 macOS、Windows、Postgres、Docker 与生命周期五项检查。修复 PR 为 [#178](https://github.com/erich04/ai-devflow-studio/pull/178)，仍是草稿。

| Issue | 已实现并验证的内容 | 剩余验收 |
| --- | --- | --- |
| #171 | 左侧流程/节点、右侧会话顶对齐；独立滚动、可调分隔线、六阶段导航；原生尺寸/主题/缩放检查通过 | 真实案例交付和验收最后两步 |
| #172 | 当前 Run、项目、策略预算、同步四组状态；状态范围与未知金额区分；有边界的浮层 | 真实 GitHub 交付后的状态核对 |
| #174 | 常驻摘要、概览/内容与审查/产物与证据/执行记录；Gate 核对清单、原文定位、历史回退、显式修订、误报反馈 | 完整真实流程收尾；不把 AI 意见当成人工审批 |
| #175 | 会话用量移至头部，部分/未知/零值分别展示；精简输入区；原生布局通过 | 最终真实流程复核 |
| #176 | sql.js 1.14.2、按写入版本刷新、空闲跳过/降频、恢复唤醒、审批失败后只读对账 | 真实交付收尾；后台恢复的独立验证与长时采样限制见下文 |
| #177 | 按 Markdown 语义分组；设计三分组；正文、依据和执行记录分离；后续阶段内容可达 | 最终真实交付及业务验收内容检查 |
| #173 | [逐文件清单](../engineering/document-localization-20260926.md)的 171 份中文化/校对完成；知识样例同步；本地链接/旧锚点与 GitHub 代表性页面渲染通过 | 最终提交 CI 与合并；不会把候选分支等同于默认分支已交付 |
| #135 | 异步凭据提示和恢复机制保留；提示占正常布局 | 用户确认暂无正式 Developer ID 签名包或发布环境，保留待验证 |

## 自动化验证

| 范围 | 实际结果 |
| --- | --- |
| `35f9981` 全量本地验证 | 312 个测试文件通过、1 个跳过；4153 项通过、15 项跳过；类型、构建与跨平台检查通过 |
| `6884d09` 审批失败新增回归 | App 155 项与工作流 9 项通过，共 164 项；桌面类型检查通过；最新 CI 五项通过 |
| 浏览器端到端测试 | 41 项通过 |
| 原生编码 Electron 冒烟测试 | 真实主进程、受控本地模型、显式权限、托管工作树改动、测试与证据保存通过；原始仓库未改动 |
| 会话 Electron 冒烟测试 | 真实 Main/IPC/SQLite，24 次受控响应，8 个节点四页签，取消/重试、会话隔离、重启后的历史/草稿恢复通过 |
| 打包 GitHub 交付冒烟测试 | 本地模拟 API 与裸仓库，非强制推送一次、草稿 PR 一次、重启不重复；验收、协调取消、绑定撤销检查通过；不调用外部模型或 GitHub |
| 连续查询 | 真实 WASM 100 万次查询与 LocalStore 100 万次读取通过，栈稳定；写入、关闭、重开后数据保留 |
| 页签首击竞态 | 修复后连续 20 次重复场景通过；页签重置在绘制前完成 |

新增审批回归覆盖三种前端结果：保存状态未知、明确未保存后由用户重试、实际已保存但 IPC 返回失败。失败后只读加载状态，不自动重试审批；只有匹配的成功节点和批准事件共同存在才显示“已保存”。真实 LocalStore 回归以不可写数据库目标触发持久化失败，验证 Run、产物、事件、发件箱与写入版本均不前进；恢复后显式重试仅产生一次审批，冷启动后仍存在，重复审批被拒绝。

受控 Electron 测试使用独立临时数据和测试凭据。生产凭据代码没有降级。模拟模型结果不作为真实模型调用证据。

## 文档中文化批次

第一批文档提交 `3d642c0` 的 CI 在 macOS 和 Windows 各发现四个旧英文知识标题断言；界面已按中文知识标题显示，其他 4153 项通过。随后更新桌面测试的精确标题和搜索词，保持索引刷新、引用、项目切换隔离与错误恢复检查。完整桌面页面 155 项及文档/知识相关 168 项本地回归通过，发布验收解析器的历史固定字段检查亦通过。新提交仍需独立 CI，不把前一代码提交的成功套用到新文档提交。

第二批提交 `aff7c93` 的 macOS 与 Windows CI 均仅因一条旧英文视觉验收文案断言失败；Docker、Docker 生命周期和 Postgres 通过。已将断言更新为文档中的中文原句，仍要求全部项目为 pass 才能宣称视觉验收完成。随后全部脚本测试本地通过：55 个文件、389 项通过，1 个文件的 15 项专用 Postgres 测试跳过；新增历史验收翻译另通过发布解析与视觉文档回归。后续 `f20f995` 的 CI 36252362792 五项全部通过（macOS、Windows、Postgres、Docker、Docker 生命周期）。

逐文件清单已完成 171/171 份。历史方案明确与新布局区分；真实服务商发布门禁纠正了与 V1.5 当前契约冲突的宽泛授权。核对 GitHub Release、标签和不可变证据后，将路线图过期的“V2.3 候选签收中”改为 2026-09-14 已发布，保持已发布包与后续分支修复的区别。发布程序读取的历史英文固定记录保留原文并解释，其余正文中文化。第三方面试题索引保留出处、题号及技术名称，明确其不是产品能力记录。

操作指南提交 `dc87665` 的 CI 36255762510 五项通过。后续 `28c2716` 的 CI 36257531439 有四项通过，macOS 因 Gate 审批界面测试未等待异步策略加载而失败；测试当时仍显示“正在加载 Gate Enforcement”，不能把该轮记为通过。已补齐等待按钮出现且可用，再保留原审批调用、可信写入、失败重试及不重复提交断言；完整 App 的 155 项回归通过。最终文档批次的全部脚本测试 55 个文件、389 项通过，1 个文件的 15 项专用 Postgres 测试跳过。新提交仍需独立 CI。

相对链接与页内锚点审计覆盖 173 份 Markdown（含本次新增的 2 份记录）、530 个链接和 89 个图片引用，未发现缺失；对照 GitHub 标题生成规则检查全部 171 份基线文档，补齐 8 个旧标题锚点后无遗漏。普通英文正文扫描剩余项均为 URL、保留的历史输入/测试输出及来源标题。

GitHub 实际页面抽查覆盖全部 6 份 README，以及路线图、新手操作手册、编码执行栈和 ADR 0022，共 10 份文档。检查了中文标题、正文、表格、折叠图集和版本边界：README 的 12 张产品截图、设计参考的 7 张截图及手册的 25 张截图均已实际加载；README 两幅和编码执行栈一幅 Mermaid 均由 GitHub 正常渲染，并检查了画面。此前 3 幅 Mermaid 的独立 Chrome 渲染也通过。此处是代表性人工渲染抽查，不声称 173 份文件均逐页截图。

实际渲染发现中文加粗末尾含冒号或右括号时，部分标记会显示成字面星号。修正 README 后，按 Markdown 语法树扫描全部文档，另修复 11 份文档的 28 个文本片段；代码块和原始输出未改动。修正后全量扫描无未解析的双星号，4 个受影响文档回归文件的 15 项测试通过。最新提交仍需独立 CI；#173 在合并和最终验收前保持 open。

## 原生布局、恢复与重新加载

最终 `6884d09` 打包应用在真实数据库的隔离副本上运行，副本断开远程服务且不含可调用的配对/模型凭据。

- 1920×1080、1440×900、1366×768、1180×768 的深浅主题，以及 1440×900 和 1180×768 的 125% 缩放，共 12 组通过。
- 主题使用实际界面按钮切换；截图取自原生 `capturePage`，不使用缩放后被裁剪的截图充当验证。
- 两栏顶对齐，全页面无横向溢出，六阶段入口可达，状态栏和常驻页签不溢出，正文可滚动、底部动作可达。
- 鼠标/键盘分隔线调整、会话用量浮层、Escape 关闭与焦点恢复通过。
- 隐藏 10 秒后恢复，再观察 60 秒，窗口保留且可见；重新加载前后 Run、产物、事件、测试、项目和用量数据一致。
- 最窄且放大场景正文可视面积较小，需要滚动；没有把这一点描述为宽屏同等阅读体验。

产物：`output/native-zoom-restore-20260926-v2/result.json` 和同目录 12 张原生截图；恢复验证另见 `output/native-zoom-restore-20260926/result.json`。

## 后台查询与栈空间对比

使用原版 sql.js 1.14.1 包和 `a876dd8` 候选包，各自读取真实业务数据的独立副本。副本移除了模型凭据、配对凭据与同步发件箱，断开 API；测量的是未配对、无交付任务的空闲场景，不能外推为全部真实联网负载。

| 场景 | 原版查询次数 | 候选查询次数 | 原版栈空间变化 | 候选栈空间变化 |
| --- | ---: | ---: | ---: | ---: |
| 前台约 3 分钟 | 798 | 74 | -12,768 字节 | 0 |
| 后台约 30 分钟 | 7,980 | 211 | -127,680 字节 | 0 |

后台样本 CPU 均值分别约 0.07871% 与 0.01210%；运行时主机还有其他测试任务，不将其作为独立性能基准。样本内内存读数会波动，未据此声称消除了所有内存泄漏。

30 分钟后台阶段已完成。原联合脚本恢复窗口后的首个采样因窗口不存在而中断，未生成完整恢复阶段结果；原因尚未确定，不把它算成通过。之后在 `6884d09` 包上独立执行的隐藏/恢复/60 秒观察通过，未复现窗口消失。完整采样和阶段边界保留在 `output/idle-soak-20260926/samples.jsonl`、`completed-phases.json`。

## 真实小需求验证

案例为真实中文任务清单仓库中的“全部/未完成/已完成”筛选。数据来自真实 Electron、SQLite、API/Postgres 和已保存 DeepSeek 配置；没有用伪造工作流进度代替执行。

1. 原需求澄清与设计已存在。解锁后真实 DeepSeek 门禁审查成功；两条与现有正文不符的中等意见通过界面提交误报反馈，保留其原版本依据与审计。
2. 经界面明确通过方案 Gate，进入开发实现。
3. 真实 DeepSeek 编码调用使用 20,184 tokens；在托管工作树中变更 6 个文件，99 行新增、3 行删除；原始仓库保持干净。权限决定、差异、测试和费用均已保存。
4. 在实际生成页面上执行 21 项浏览器检查，覆盖三个筛选、三种空态、即时勾选/取消、隐藏任务的全局清除、刷新复位、任务持久化、中文和原有功能，全部通过。
5. 通过正式测试节点运行保存的命令，保存真实测试证据，Run 进入 v9；随后生成 PR 交付包，Run 为 v10。
6. 当前停在 PR 交付。项目尚未绑定 GitHub 仓库，首次准备交付收到 `binding_observation_rejected`；没有创建交付意图，没有推送或创建真实草稿 PR。
7. 已核实目标现有 App 安装与仓库并准备绑定表单，等待用户确认新增项目交付授权。后续 Web 精确审批、真实发布、草稿 PR 对账和业务验收均未完成。

因此尚不能声称真实全流程完成。

## 本机安装与数据保留

2026-09-26 14:42 UTC 升级前备份 SQLite 和配置，正常退出原应用后，将 `6884d09` 包安装到用户原路径，沿用原数据目录、配对与模型设置。候选包归档 SHA-256 为 `e8027b298843199f39c0cb5931b0e65d46c039aa6490cc1da256de4fa06e783d`。

重启后 SQLite 完整性检查为 `ok`。逐表指纹比对：Run、节点、产物、审批事件、测试、编码记录和加密凭据完全一致；5 个会话仍在，仅当前会话版本号与更新时间发生变化，消息内容未改变。界面显示 Run v10、PR 交付，当前节点产物 1、轨迹 1；原安装包和数据库备份保留，未重置数据。

原锁屏/钥匙串问题已由用户手动解锁后恢复，不能再把锁屏作为当前阻塞。正式 Developer ID 签名安装的独立验收仍缺失，#135 按用户明确要求保持 open。

## 关闭条件

完成真实交付与业务验收，核对本地、团队投影及 GitHub 结果；再按各 Issue 的验收项引用提交和证据逐项决定关闭。#173 必须完成逐文件中文化、知识样例一致性与渲染验收；#135 在正式签名安装验证前不能关闭。
